# Experiments

Each experiment directory should contain:

```text
config.yaml
notes.md
metrics.json
samples/
plots/
checkpoint_reference.txt
```

Track experiments as:

```text
hypothesis -> config -> run -> metrics -> samples -> conclusion -> next step
```

Initial tracked experiment:

- `exp_000_environment_sanity`
